---
phase: 7
plan: 07-02
name: Integration and End-to-End Tests
wave: 1
depends_on:
  - 07-01
requirements:
  - REQ-CALC-008 (full coverage)
files_modified:
  - tests/test_integration.py
  - tests/test_e2e.py
---

# Plan 07-02: Integration and End-to-End Tests

**Type**: feature
**Wave**: 1
**Depends on**: Plan 07-01 (calculation unit tests)

## Tasks

<task>
<type>feature</type>
<description>Create integration tests for full calculation pipeline</description>
<read_first>
- /home/olebinders/Documents/Code/Induction Heating/.planning/phases/07-validation-testing/07-RESEARCH.md
</read_first>
<acceptance_criteria>
- Test full pipeline: setup → calculate → verify results are physically reasonable
- Skin depth decreases with frequency
- Skin depth increases with resistivity
- Skin depth decreases with permeability
- Total power increases with frequency
- Total power increases with current
- Power density peaks at workpiece surface (skin effect)
- Permeability drops to 1.0 at Curie point
</acceptance_criteria>
<verify>
- All integration tests pass
- Results are physically reasonable (positive values, correct trends)
</verify>
</task>

<task>
<type>feature</type>
<description>Create end-to-end GUI tests</description>
<read_first>
- /home/olebinders/Documents/Code/Induction Heating/.planning/phases/07-validation-testing/07-RESEARCH.md
</read_first>
<acceptance_criteria>
- Full workflow: set parameters → click Run → verify results display
- Material change: select different material → verify property plot updates
- Save/load: save simulation → create new window → load → verify panels populated
- View toggle: switch between B-field and power density views
- Validation error: set invalid parameters → verify error message and disabled Run button
</acceptance_criteria>
<verify>
- All end-to-end GUI tests pass
- Full workflow produces visible results in all panels
</verify>
</task>

<task>
<type>test</type>
<description>Run full test suite and verify final pass rate</description>
<acceptance_criteria>
- All tests pass (unit + integration + GUI + validation)
- Total test count > 260
- No test failures or errors
- Test coverage covers all calculation modules
</acceptance_criteria>
<verify>
- `pytest tests/ -v` passes all tests
- Final test count > 260
</verify>
</task>

## Artifacts this phase produces

- `tests/test_integration.py` — Integration tests for calculation pipeline
- `tests/test_e2e.py` — End-to-end GUI tests

## Must Haves

**Truths:**
- Full pipeline produces physically reasonable results
- GUI workflow works end-to-end
- Save/load round-trip preserves all parameters
- All validation tests pass

**Prohibitions:**
- MUST NOT skip any validation tests
- MUST NOT mock calculation functions in integration tests
- MUST test actual file I/O for save/load

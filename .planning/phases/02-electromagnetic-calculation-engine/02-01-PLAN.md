---
phase: 2
plan: 02-01
name: Geometry System
wave: 1
depends_on: []
requirements:
  - REQ-GEO-001
  - REQ-GEO-002
  - REQ-GEO-003
  - REQ-GEO-004
files_modified:
  - src/induction_heating/core/geometry.py
  - tests/test_geometry.py
---

# Plan 02-01: Geometry System

**Type**: feature
**Wave**: 1
**Depends on**: Phase 1 (material library, units)

## Tasks

<task>
<type>feature</type>
<description>Create SolenoidCoil dataclass with validation</description>
<read_first>
- /home/olebinders/Documents/Code/Induction Heating/.planning/phases/02-electromagnetic-calculation-engine/02-RESEARCH.md
- /home/olebinders/Documents/Code/Induction Heating/.planning/research/PITFALLS.md
</read_first>
<acceptance_criteria>
- SolenoidCoil dataclass with fields: inner_radius, outer_radius, length, turns, wire_diameter
- All dimensions are positive floats (validated)
- Turns must be positive integer
- Wire diameter must be positive
- Length must be > 0
- inner_radius must be > 0
- outer_radius must be >= inner_radius
</acceptance_criteria>
<verify>
- Valid solenoid parameters create successfully
- Invalid parameters (negative radius, zero turns) raise ValueError
- Mean radius computed as (inner_radius + outer_radius) / 2
- Turn density (turns per unit length) computed correctly
</verify>
</task>

<task>
<type>feature</type>
<description>Create CylindricalWorkpiece dataclass with validation</description>
<read_first>
- /home/olebinders/Documents/Code/Induction Heating/.planning/phases/02-electromagnetic-calculation-engine/02-RESEARCH.md
</read_first>
<acceptance_criteria>
- CylindricalWorkpiece dataclass with fields: radius, length, material_name
- Radius must be > 0
- Length must be > 0
- material_name must be a non-empty string
- Cross-sectional area computed as πr²
- Volume computed as πr²L
</acceptance_criteria>
<verify>
- Valid workpiece parameters create successfully
- Invalid parameters raise ValueError
- Cross-sectional area and volume computed correctly
</verify>
</task>

<task>
<type>feature</type>
<description>Create InductionSetup dataclass combining coil + workpiece with gap validation</description>
<read_first>
- /home/olebinders/Documents/Code/Induction Heating/.planning/phases/02-electromagnetic-calculation-engine/02-RESEARCH.md
</read_first>
<acceptance_criteria>
- InductionSetup dataclass with fields: coil (SolenoidCoil), workpiece (CylindricalWorkpiece), gap (radial clearance between coil inner radius and workpiece radius)
- Gap must be >= 0
- Workpiece radius must be < coil inner radius (workpiece fits inside coil)
- Workpiece length must be <= coil length (or warning if longer)
- Coupling factor computed from geometry
</acceptance_criteria>
<verify>
- Valid setup creates successfully
- Workpiece larger than coil inner radius raises ValueError
- Negative gap raises ValueError
- Coupling factor is between 0 and 1
</verify>
</task>

<task>
<type>test</type>
<description>Create unit tests for geometry classes</description>
<acceptance_criteria>
- Tests for valid SolenoidCoil creation
- Tests for invalid SolenoidCoil parameters (negative radius, zero turns, etc.)
- Tests for valid CylindricalWorkpiece creation
- Tests for invalid CylindricalWorkpiece parameters
- Tests for valid InductionSetup creation
- Tests for invalid InductionSetup (workpiece too large, negative gap)
- Tests for computed properties (mean radius, turn density, area, volume, coupling)
</acceptance_criteria>
<verify>
- `pytest tests/test_geometry.py -v` passes all tests
</verify>
</task>

## Artifacts this phase produces

- `src/induction_heating/core/geometry.py` — SolenoidCoil, CylindricalWorkpiece, InductionSetup
- `tests/test_geometry.py` — Geometry tests

## Must Haves

**Truths:**
- All geometry dimensions are validated before use
- Workpiece fits inside coil (radius + gap < coil inner radius)
- Coupling factor reflects geometric relationship between coil and workpiece

**Prohibitions:**
- MUST NOT allow negative or zero dimensions
- MUST NOT allow workpiece larger than coil inner radius
- MUST NOT allow negative gap
